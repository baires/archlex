<div class="package-quickstart">
  <p>Browser + Node.js · AWS + GCP + Kubernetes · Open source (MIT)</p>
  <pre><code>npm install @archlex/core @archlex/aws @archlex/gcp @archlex/k8s</code></pre>
  <pre><code>const result = await archlex.render(source);
document.querySelector("#diagram").innerHTML = result.svg;</code></pre>
</div>
