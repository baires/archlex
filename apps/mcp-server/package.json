{
  "name": "@archlex/mcp-server",
  "version": "0.1.6",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "sync:docs": "node scripts/sync-docs.mjs",
    "predev": "pnpm sync:docs",
    "dev": "wrangler dev",
    "prebuild": "pnpm sync:docs",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "pretypecheck": "pnpm sync:docs",
    "typecheck": "tsc --noEmit",
    "pretest": "pnpm sync:docs",
    "test": "vitest run"
  },
  "dependencies": {
    "@archlex/aws": "workspace:*",
    "@archlex/core": "workspace:*",
    "@archlex/gcp": "workspace:*",
    "@archlex/layout-elk": "workspace:*",
    "@archlex/model": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.5.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250204.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5",
    "wrangler": "^4.120.0"
  }
}
